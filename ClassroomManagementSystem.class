// Source code is decompiled from a .class file using FernFlower decompiler.
import java.util.ArrayList;
import java.util.Iterator;

class ClassroomManagementSystem {
   private ArrayList<Student> students = new ArrayList();

   public ClassroomManagementSystem() {
   }

   public void addStudent(String var1, String var2, String var3, String var4, double var5) {
      Student var7 = new Student(var1, var2, var3, var4, var5);
      this.students.add(var7);
      System.out.println("Student added: " + String.valueOf(var7));
   }

   public void displayStudents() {
      if (this.students.isEmpty()) {
         System.out.println("No students to display.");
      } else {
         System.out.println("List of Students:");
         Iterator var1 = this.students.iterator();

         while(var1.hasNext()) {
            Student var2 = (Student)var1.next();
            System.out.println(var2);
         }

      }
   }

   public void deleteStudent(String var1) {
      for(int var2 = 0; var2 < this.students.size(); ++var2) {
         if (((Student)this.students.get(var2)).getStudentId().equals(var1)) {
            this.students.remove(var2);
            System.out.println("Student with ID " + var1 + " has been deleted.");
            return;
         }
      }

      System.out.println("Student with ID " + var1 + " not found.");
   }
}
